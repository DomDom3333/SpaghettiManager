<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="SpaghettiManager.App.Views.InventoryActionView"
    x:DataType="viewModels:InventoryActionViewModel"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:viewModels="clr-namespace:SpaghettiManager.App.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            <Label FontAttributes="Bold" FontSize="20" Text="{Binding Title}" />
            <Label FontSize="12" Text="{Binding Description}" />

            <VerticalStackLayout IsVisible="{Binding ShowCatalogFields}" Spacing="12">
                <Picker
                    ItemsSource="{Binding ManufacturerOptions}"
                    SelectedItem="{Binding Manufacturer}"
                    Title="Manufacturer" />
                <Picker
                    ItemsSource="{Binding ProductLineOptions}"
                    SelectedItem="{Binding ProductLine}"
                    Title="Product line" />
                <Picker
                    ItemsSource="{Binding MaterialOptions}"
                    SelectedItem="{Binding Material}"
                    Title="Material" />
                <Picker
                    ItemsSource="{Binding ColorOptions}"
                    SelectedItem="{Binding ColorName}"
                    Title="Color" />
            </VerticalStackLayout>

            <Picker
                ItemsSource="{Binding CarrierOptions}"
                SelectedItem="{Binding SpoolCarrier}"
                Title="Spool / carrier" />
            <Entry Keyboard="Numeric" Placeholder="Total weight (g)" Text="{Binding TotalWeight}" />
            <Entry Keyboard="Numeric" Placeholder="Empty spool weight (g)" Text="{Binding EmptySpoolWeight}" />
            <Editor AutoSize="TextChanges" Placeholder="Notes" Text="{Binding Notes}" />

            <Button Command="{Binding SaveCommand}" Text="Save" />
        </VerticalStackLayout>
    </ScrollView>
</ContentView>
