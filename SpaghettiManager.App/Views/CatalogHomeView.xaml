<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="SpaghettiManager.App.Views.CatalogHomeView"
    x:DataType="viewModels:CatalogHomeViewModel"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:viewModels="clr-namespace:SpaghettiManager.App.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            <Label FontAttributes="Bold" Text="Catalog" />
            <CollectionView ItemsSource="{Binding Sections}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:CatalogHomeViewModel.CatalogSectionItem">
                        <Frame Margin="0,4" Padding="12">
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Spacing="2">
                                    <Label FontAttributes="Bold" Text="{Binding Title}" />
                                    <Label FontSize="12" Text="{Binding Description}" />
                                </VerticalStackLayout>
                                <Button
                                    Command="{Binding Source={RelativeSource AncestorType=ContentView}, Path=BindingContext.OpenSectionCommand}"
                                    CommandParameter="{Binding .}"
                                    Grid.Column="1"
                                    Text="Open" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>
